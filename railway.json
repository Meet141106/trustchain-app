{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd blockchain && npx hardhat node --hostname 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
